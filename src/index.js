import React from "react";
import Pagination from "./components/Pagination";
import usePaginationParams from "./hooks/usePaginationParams";

/**
 * Entry point for exporting components and hooks related to pagination.
 * Exports Pagination component and usePaginationParams hook.
 */
export { Pagination, usePaginationParams };
